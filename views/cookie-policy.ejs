<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PICASF - Cookie Policy</title>
  <meta name="description" content="Learn about PICASF Consulting's cookie policy and how we use cookies to improve your experience.">
  <meta name="author" content="PICASF Consulting">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Basic styling for body */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f9;
      color: #333;
    }

    /* Cookie consent banner */
    .cookie-consent-banner {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background-color: #333;
      color: white;
      padding: 20px;
      font-size: 1rem;
      text-align: center;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
      z-index: 9999;
      font-family: 'Roboto', sans-serif;
    }

    .cookie-consent-banner a {
      color: #f1c40f;
      text-decoration: none;
      font-weight: bold;
    }

    .cookie-consent-banner a:hover {
      text-decoration: underline;
    }

    /* Styling for buttons */
    .cookie-consent-banner button {
      margin-left: 15px;
      padding: 10px 20px;
      border-radius: 5px;
      font-weight: bold;
    }

    #accept-cookies {
      background-color: #28a745;
      border: none;
      color: white;
    }

    #reject-cookies {
      background-color: #dc3545;
      border: none;
      color: white;
    }

    /* Modal styling */
    .modal-content {
      background-color: #2c3e50;
      color: white;
      border-radius: 8px;
      padding: 20px;
    }

    .modal-header {
      background-color: #34495e;
      border-radius: 8px 8px 0 0;
    }

    .modal-footer {
      background-color: #34495e;
      border-radius: 0 0 8px 8px;
    }

    .modal-title {
      font-size: 1.5rem;
      font-weight: bold;
    }

    .modal-body {
      font-size: 1rem;
      line-height: 1.6;
    }

    .modal-body h2 {
      color: #f1c40f;
      font-size: 1.3rem;
      margin-bottom: 10px;
    }

    /* Fade-in Animation */
    .modal.fade .modal-dialog {
      transform: translateY(-50%);
      opacity: 0;
      animation: fadeIn 0.5s forwards;
    }

    @keyframes fadeIn {
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
  </style>
</head>
<body>

  <!-- Cookie Consent Banner -->
  <div id="cookie-consent-banner" class="cookie-consent-banner">
    <p class="m-0">
      We use cookies to improve your experience on our site. By accepting, you agree to our <a href="#" id="cookie-policy-link">cookie policy</a>.
    </p>
    <button id="accept-cookies">Accept</button>
    <button id="reject-cookies">Reject</button>
  </div>

  <!-- Cookie Policy Modal -->
  <div class="modal fade" id="cookiePolicyModal" tabindex="-1" aria-labelledby="cookiePolicyModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="cookiePolicyModalLabel">Cookie Policy</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <h2>What Are Cookies?</h2>
          <p>Cookies are small text files that are stored on your device when you visit a website. They are used to make websites function more efficiently and to provide relevant information to the site owners.</p>

          <h2>Why We Use Cookies</h2>
          <p>We use cookies to enhance your experience by:</p>
          <ul>
            <li>Remembering your preferences for future visits.</li>
            <li>Analyzing website traffic to improve our content.</li>
            <li>Personalizing content based on your interests.</li>
          </ul>

          <h2>Types of Cookies We Use</h2>
          <p>We use the following types of cookies:</p>
          <ul>
            <li><strong>Essential Cookies:</strong> Required for the website to function.</li>
            <li><strong>Performance Cookies:</strong> Help us analyze how users interact with the website.</li>
            <li><strong>Functional Cookies:</strong> Remember user preferences and settings.</li>
            <li><strong>Targeting Cookies:</strong> Used to display personalized ads.</li>
          </ul>

          <h2>Manage Your Cookies</h2>
          <p>You can change your cookie settings through your browser's preferences or through our website.</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="button" class="btn btn-primary" id="accept-policy">Accept Cookies</button>
        </div>
      </div>
    </div>
  </div>

  <!-- JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Show the cookie policy modal when the link is clicked
    document.getElementById('cookie-policy-link').addEventListener('click', function(e) {
      e.preventDefault();
      var modal = new bootstrap.Modal(document.getElementById('cookiePolicyModal'));
      modal.show();
    });

    // Accept Cookies Action
    document.getElementById('accept-policy').addEventListener('click', function() {
      document.getElementById('cookie-consent-banner').style.display = 'none';
      localStorage.setItem('cookiesAccepted', 'true');
    });

    // Reject Cookies Action
    document.getElementById('reject-cookies').addEventListener('click', function() {
      document.getElementById('cookie-consent-banner').style.display = 'none';
      localStorage.setItem('cookiesAccepted', 'false');
    });

    // Check if the user has already accepted or rejected cookies
    if (localStorage.getItem('cookiesAccepted') === null) {
      document.getElementById('cookie-consent-banner').style.display = 'block';
    } else {
      document.getElementById('cookie-consent-banner').style.display = 'none';
    }
  </script>
</body>
</html>
