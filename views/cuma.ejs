<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Joseph Chuma - My Guitar and Tourism</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #000;
      color: #fff;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      line-height: 1.6;
      scroll-behavior: smooth;
    }

    /* Fullscreen Hero Section */
    .hero-section {
      position: relative;
      width: 100%;
      min-height: 100vh; /* Ensures proper height on mobile */
      background: url('https://picasf.com/images/cuma.jpg') center center no-repeat;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }

    /* Text Box with semi-transparent background */
    .overlay {
      background: rgba(0, 0, 0, 0.6); /* Black with 60% opacity */
      padding: 20px;
      border-radius: 10px;
      display: inline-block;
      max-width: 80%;
    }

    .overlay h1 {
      font-size: 4rem;
      font-weight: bold;
      letter-spacing: 5px;
      text-transform: uppercase;
    }

    .overlay p {
      font-size: 1.5rem;
      font-weight: lighter;
      margin-top: 20px;
    }

    /* Responsive text adjustments */
    @media (max-width: 767px) {
      .overlay {
        max-width: 90%; /* Ensure text stays inside on small screens */
        padding: 15px;
      }

      .overlay h1 {
        font-size: 2.5rem;
      }

      .overlay p {
        font-size: 1.1rem;
      }

      .hero-section {
        padding: 40px 10px; /* More space on mobile */
        background-position: center top; /* Better focus on main image */
      }
    }

    /* Navigation */
    .navbar {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      background: rgba(0, 0, 0, 0.5);
      padding: 10px 0;
      z-index: 100;
    }

    .navbar a {
      color: #fff;
      font-weight: 700;
      font-size: 1.1rem;
      text-transform: uppercase;
      padding: 0 20px;
      transition: all 0.3s ease;
    }

    .navbar a:hover {
      color: #4caf50;
      text-decoration: none;
    }
  </style>
</head>

<body>

  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="#">Joseph Chuma</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="#about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#read">Read</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#contact">Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="hero-section">
    <div class="overlay">
      <h1>JOSEPH CHUMA FISTON</h1>
      <p>Explore the journey of music, culture, and connection with Joseph Chuma</p>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>

</body>
</html>
